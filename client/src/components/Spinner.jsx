import React from "react";

const Spinner = () => {
	return (
		// copied from a tutorial...change later
		<div className="animate-ping w-16 h-16 m-8 rounded-full bg-sky-600"></div>
	);
};

export default Spinner;
